# TAdev2
Diagnostic build for an owned/test iOS device.

After installation and userspace reboot, launch the target app and reproduce the camera connection/live-view flow. The probe writes observed NSURLSession URLs to `/var/mobile/TAdev2.log`.
